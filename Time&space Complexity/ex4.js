// Find the complexicity of
let a = 0,
    b = 0;

for (i = 0; i < N; i++) {  //O(N)
  //O(N)
  a = a + Math.random();
}

for (j = 0; j < M; j++) { //O(M)
  //O(M)
  b = b + Math.random();
}

//time= O(N)+O(M)=O(N+M)
//space=O(1)
